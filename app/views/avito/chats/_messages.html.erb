<div class="mb-3">
  <div class="list-messages" data-controller="fancybox auto-scroll-down" style="height: calc(100vh - 350px);">
    <ul class="list-group">
      <% @messages.each do |message| %>
        <%= render partial: 'message', locals: { message: message } %>
      <% end %>
    </ul>
  </div>
</div>

<%= render partial: '/avito/chats/form' %>
