<div class="container">
  <h1><%= @product.title %></h1>
  <ul class="list-group">
    <% colum_names = @product.attributes.keys[1..-3] %>
    <% colum_names.each do |name| %>
      <li class="list-group-item list-group-item-action">
        <strong><%= name %>: </strong><%= @product.send(name.to_sym) %>
      </li>
    <% end %>
    <li class="list-group-item list-group-item-action">
      <strong>image: </strong>
      <% if @product.image.attached? %>
          <a href='<%= url_for(@product.image) %>'>
            <%= image_tag url_for(@product.image), style: 'height: 100px;' %>
          </a>
      <% end %>
    </li>
  </ul>
  <a href="<%= edit_product_path(@product) %>" class='btn btn-primary'>Edite</a>
</div>